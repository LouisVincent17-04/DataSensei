@once
<style id="datasensei-admin-inspired-page-style">
  /*
   * Kept for the student and instructor pages that include it (body class
   * "ds-admin-inspired"). The shared look now comes from partials.design-system
   * and each page's own styles, so this only guarantees the flat page
   * background those screens were designed around.
   */
  body.ds-admin-inspired {
    background-color: var(--bg, #0d1320);
    background-image: none;
    color: var(--text, #f8fafc);
  }
</style>
@endonce
